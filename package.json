{
  "name": "@kazion/dpopay-sdk",
  "version": "3.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsup",
    "test": "vitest",
    "test:cov": "vitest --coverage",
    "clean": "rm -rf ./lib",
    "audit:fix": "yarn-audit-fix --force"
  },
  "tsup": {
    "entry": [
      "src/index.ts"
    ],
    "format": [
      "cjs",
      "esm"
    ],
    "outDir": "lib",
    "splitting": false,
    "sourcemap": false,
    "dts": true,
    "minify": true,
    "clean": true,
    "watch": true
  },
  "files": [
    "lib"
  ],
  "devDependencies": {
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.6.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.4",
    "vitest": "^1.2.2",
    "yarn-audit-fix": "^10.0.7"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "fast-xml-parser": "^4.2.1"
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "dpo",
    "dpo africa",
    "dpo-sdk",
    "dpo-sdk-node",
    "dpo-sdk-nodejs"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/patrickkabwe/dpo-node-sdk"
  },
  "release": {
    "branches": [
      "main",
      "next",
      {
        "name": "beta",
        "prerelease": true
      }
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      [
        "@semantic-release/git",
        {
          "assets": [
            "package.json",
            "lib/*",
            "src/*"
          ],
          "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
        }
      ],
      "@semantic-release/github",
      "@semantic-release/npm"
    ]
  }
}
